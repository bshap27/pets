<%= form_for(@user_breed) do |f| %>
  <% if @user_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_breed.errors.count, "error") %> prohibited this user_breed from being saved:</h2>

      <ul>
      <% @user_breed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
